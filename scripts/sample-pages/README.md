# Sample page photos

Put 2+ photos of book pages here (jpg/png/webp), e.g.:

    page-1.jpg
    page-2.jpg

Then run `npm run make-samples` to pre-generate the "original" sample cards
(stills + moving loops + dialogue TTS) into `public/samples/`.
Pages already in the manifest are skipped, so the script is resumable.
